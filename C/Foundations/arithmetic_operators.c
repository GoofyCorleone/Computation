#include <stdio.h>

int main(){
    /*
        Arithmetic operators:
        "="
        "+"
        "-"
        "*"
        "/"
        "%"
        "++"
        "--"
    */

    int x = 10;
    int y = 3;
    int z = 0;

    // z = x + y;
    // z = x-y;
    // z = x * y;
    // z = x / y; // Integers cannot storage decimal points
    // z = x % y; // The modulus operator

    // x++;
    // x--;
    // x += 2;
    // x -= 10;
    // x *= 10;
    // x /= 3;
    // x %= 5;
    
    printf("%d\n", z);
    printf("%d\n", x);

    return 0;
}